"""
Utility scripts for building/searching the local doc index (RAG).

This file makes the `tools` directory importable as a package, so higher-level
agents can reuse retrieval helpers (e.g. `tools.rag_ask.load_index`).
"""



